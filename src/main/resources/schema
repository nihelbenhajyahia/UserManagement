DROP TABLE IF EXISTS users;

CREATE TABLE userEntity(
    id INT AUTO_INCREMENT PRIMARY KEY,
    name varchar(255),
    birthdate Date,
    country varchar(255),
    phone varchar(255), 
    gender varchar(255)
	);
	